<%= form_with(model: screening_type, local: true) do |form| %>
  <% if screening_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(screening_type.errors.count, "error") %> prohibited this screening_type from being saved:</h2>

      <ul>
      <% screening_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :time_minutes %>
    <%= form.number_field :time_minutes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
